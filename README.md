# Moodify
An AI-powered music app that creates personalized playlists from a user’s mood and context, continuously improving recommendations through stored mood history.

> ⚠️ This project requires an OpenAI API key to function.  
> Users must create their own API key from https://platform.openai.com/api-keys and add it to a `.env` file as `OPENAI_API_KEY`.
